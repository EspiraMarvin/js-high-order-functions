//          JAVASCRIPT ARRAY METHODS

//     shift array method (opposite of the pop method)
// Removes the first item from an array and returns that removed item

const numbers = [1,2,3];

const firstItem = numbers.shift();

console.log(numbers);
console.log(firstItem);
